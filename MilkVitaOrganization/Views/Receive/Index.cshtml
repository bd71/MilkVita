@model IEnumerable<MilkVitaOrganization.Models.tbl_Receive>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Receive</h2><br />
<head>
    <title></title>
    <script src="https://code.jquery.com/jquery-3.3.1.min.js" type="text/javascript"></script>

    <script src="https://unpkg.com/sweetalert/dist/sweetalert.min.js"></script>

    <script type="text/javascript">
        $(document).ready(function () {

            $(".myLink").click(function () {
                var val = $(this).data("id")
                if (confirm("Are you sure you want to delete this?")) {

                    $.ajax({
                        url: '@Url.Action("DeleteConfirmed", "Receive")',
                        dataType: "json",
                        type: "POST",
                        cache: false,
                        data: { Id: val },
                        success: function (data) {
                            if (data.result) {
                                alert(data.result);
                            }
                        },
                        error: function (xhr) {
                            alert(xhr.responseText);
                        }
                    });

                }
                else {
                    return false;
                }
            });

        });
    </script>
</head>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.receiveNo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GRN)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.serialNo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.dateTime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.receiveQty)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tbl_Brand.name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tbl_Color.name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tbl_Demand.demandNo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tbl_ItemMaster.name)
        </th>
        <th>
           Unit
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.receiveNo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GRN)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.serialNo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.dateTime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.receiveQty)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tbl_Brand.name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tbl_Color.name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tbl_Demand.demandNo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tbl_ItemMaster.name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tbl_Unit.name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.id }, new { @class = "myLink", data_id = item.id })
        </td>
    </tr>
}

</table>
